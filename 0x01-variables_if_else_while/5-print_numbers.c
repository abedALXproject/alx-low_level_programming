#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Description: this print 0 to 10.
 * Return: 0 (success)
*/
int main(void)
{
	int w;

	for (w = 0; w < 10; w++)
	{
		printf("%d", w);
	}
	printf("\n");
	return (0);
}
